Feature: Client and Pet Management

Scenario: Add a new client with valid ID
   Given A user logged in with valid credentials
   When A user navigate to the "Clientes" section
   #And A user press the "+" button to add a new client
   #And A user fill the client form with valid data and a valid ID
   #Then A user should see the new client in the grid

#Scenario: Add a new pet to the client
#   Given A user have selected the client with ID ""
#   When A user press the "+" button to add a new pet
#   And A user fill the pet form with valid data for the pet ""
#   Then A user should see the new pet in the grid